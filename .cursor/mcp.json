{
  "mcpServers": {
    "serial-capture-logs": {
      "command": "cargo",
      "args": ["run", "--quiet", "--bin", "serial-capture-mcp"],
      "env": {
        "SERIAL_CAPTURE_LOG_DIR": "${workspaceFolder}/logs",
        "MCP_AUDIT_PATH": "${workspaceFolder}/logs/mcp-audit.jsonl",
        "MCP_CALLER_IDENTITY": "agent:cursor-host"
      }
    }
  }
}
